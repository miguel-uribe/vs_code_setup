# Normalize text files in the repository.
* text=auto

# Shell scripts must keep LF endings or they fail on macOS and Linux
# with a "bad interpreter" error.
*.sh text eol=lf

# PowerShell is happier with CRLF on Windows.
*.ps1 text eol=crlf
